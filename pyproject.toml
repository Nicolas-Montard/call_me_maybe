[project]
name = "call-me-maybe"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "numpy",
    "llm_sdk"
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
[tool.uv.sources]
llm_sdk = { path = "llm_sdk" }